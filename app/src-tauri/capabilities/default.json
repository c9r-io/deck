{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "main window: core APIs incl. event listen (PTY stream arrives as events)",
  "windows": ["main"],
  "permissions": ["core:default"]
}
